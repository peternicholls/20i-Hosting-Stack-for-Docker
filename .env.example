# 20i Stack Environment Configuration
# Copy this file to .env and adjust values as needed

# Code directory - REQUIRED
# This should point to your website/application code
CODE_DIR=./src

# Web server configuration
HOST_PORT=80
# Use the latest stable PHP FPM Alpine tag
# Update in a future branch if a newer stable is desired
PHP_VERSION=8.5

# Database configuration
MYSQL_VERSION=10.6
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=devdb
MYSQL_USER=devuser
MYSQL_PASSWORD=devpass

# phpMyAdmin
PMA_PORT=8081
# For Apple Silicon/ARM users: use native ARM image for better performance
# PMA_IMAGE=arm64v8/phpmyadmin:latest
# For cross-platform compatibility (default):
# PMA_IMAGE=phpmyadmin/phpmyadmin:latest

# Optional: Additional environment variables
# COMPOSE_PROJECT_NAME=my-20i-project
